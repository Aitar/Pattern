spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/postgraduate?characterEncoding=utf8&useUnicode=true&useSSL=false&serverTimezone=UTC
    username: root
    password: password

  data:
    mongodb:
      host: 127.0.0.1
      port: 27017

  redis:
    port: 6379
    database: 0
    host: 10.10.10.133

mybatis:
  type-aliases-package: com.msc.pojo